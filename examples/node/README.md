# MTran Node.js Example

Demonstrates how to use MTran in a Node.js environment.

## Running

```bash
# Install dependencies
npm install

# Run the example
node index.js
```

The first run will download necessary translation models to the `./models` directory.
